{
  "questions": [
    {
      "category": "File I/O",
      "question": "Open the file `fix.log` in read mode with file object `f`:\n\n```python\n# you need to write this line\n```",
      "options": [
        "with open('fix.log', 'r') as f:",
        "open('fix.log', 'r') -> f",
        "f = open('fix.log', mode='rb')",
        "file('fix.log', 'r')"
      ],
      "answer": 0,
      "explanation": "Use a context manager with mode 'r'."
    },
    {
      "category": "File I/O",
      "question": "Iterate through the file object `f`, one line at a time:\n\n```python\n# f is open\n```",
      "options": [
        "for line in f:",
        "while not f.eof():",
        "lines = f.read().split('\\n')",
        "f.foreach(line)"
      ],
      "answer": 0,
      "explanation": "A file object is its own iterator over lines."
    },
    {
      "category": "String Parsing",
      "question": "Strip leading/trailing whitespace from the string `line`:\n\n```python\nline = '   some text   '\n# now remove the spaces\n```",
      "options": [
        "line = line.strip()",
        "line = line.lstrip()",
        "line = line.rstrip()",
        "line = line.trim()"
      ],
      "answer": 0,
      "explanation": "`strip()` removes whitespace from both ends."
    },
    {
      "category": "String Parsing",
      "question": "Split `line` into parts on `|` if present, else on SOH (`\\x01`):\n\n```python\n# assume line is a FIX message string\n```",
      "options": [
        "parts = line.split('|' if '|' in line else '\\x01')",
        "parts = re.split(r'\\||\\x01', line)",
        "if '|' in line:\n    parts = line.split('|')\nelse:\n    parts = line.split('\\x01')",
        "parts = [line]"
      ],
      "answer": 0,
      "explanation": "A conditional inside `split()` chooses the right separator."
    },
    {
      "category": "Data Structures",
      "question": "Initialize an empty list named `pairs`:\n\n```python\n# start here\n```",
      "options": [
        "pairs = []",
        "pairs = list()",
        "pairs = {}",
        "pairs = ()"
      ],
      "answer": 0,
      "explanation": "[] creates an empty list."
    },
    {
      "category": "Parsing Logic",
      "question": "Loop over each element in `parts` using variable `part`:\n\n```python\nparts = [...]  # some list\n# iterate here\n```",
      "options": [
        "for part in parts:",
        "parts.forEach(lambda part: ...)",
        "while parts:\n    part = parts.pop()",
        "foreach part in parts:"
      ],
      "answer": 0,
      "explanation": "Standard for-loop over a list."
    },
    {
      "category": "Parsing Logic",
      "question": "Only process `part` if it contains an '=' character:\n\n```python\n# inside your loop\n```",
      "options": [
        "if '=' in part:",
        "if part.contains('='):",
        "if part.index('=') >= 0:",
        "if part == '=':"
      ],
      "answer": 0,
      "explanation": "Use the `in` operator to test substring presence."
    },
    {
      "category": "Parsing Logic",
      "question": "Split `part` at the first '=' into `tag` and `value`:\n\n```python\n# part is something like '35=D'\n```",
      "options": [
        "tag, value = part.split('=', 1)",
        "tag, sep, value = part.partition('=')",
        "tag, value = part.split('=')",
        "tag, value = re.split('=', part)"
      ],
      "answer": 0,
      "explanation": "`split(sep, 1)` splits on first occurrence only."
    },
    {
      "category": "Data Structures",
      "question": "Append the `(tag, value)` tuple to `pairs`:\n\n```python\npairs = []\n# inside your loop\n```",
      "options": [
        "pairs.append((tag, value))",
        "pairs += (tag, value)",
        "pairs.add((tag, value))",
        "append(pairs, (tag, value))"
      ],
      "answer": 0,
      "explanation": "Use `list.append()` to add an element."
    },
    {
      "category": "Dict Construction",
      "question": "Build a dict `msg` from `pairs`:\n\n```python\npairs = [('35','D'), ('55','IBM')]\n# build dict here\n```",
      "options": [
        "msg = dict(pairs)",
        "msg = {k:v for k,v in pairs}",
        "msg = pairs.to_dict()",
        "msg = {}.fromkeys(pairs)"
      ],
      "answer": 0,
      "explanation": "Passing a list of (key, value) tuples to `dict()` constructs the dict."
    },
    {
      "category": "FIX Parsing",
      "question": "Extract the FIX Symbol into `symbol` from `msg`:\n\n```python\nmsg = {'55':'IBM', ...}\n# get symbol\n```",
      "options": [
        "symbol = msg['55']",
        "symbol = msg.get('55')",
        "symbol = msg.get('Symbol')",
        "symbol = msg.Symbol"
      ],
      "answer": 0,
      "explanation": "Tag 55 is the Symbol field in FIX."
    },
    {
      "category": "FIX Parsing",
      "question": "Extract the FIX Quantity into `quantity` from `msg`:\n\n```python\nmsg = {'38':'100', ...}\n# get quantity\n```",
      "options": [
        "quantity = msg['38']",
        "quantity = int(msg['38'])",
        "quantity = msg.get('38')",
        "quantity = msg.Quantity"
      ],
      "answer": 0,
      "explanation": "Tag 38 is the OrderQty field in FIX."
    },
    {
      "category": "Aggregation",
      "question": "Compute total executed quantity for order `'ORD1'`:\n\n```python\nfills = {'ORD1': [20, 30, 50]}\n# compute total\n```",
      "options": [
        "total = sum(fills['ORD1'])",
        "total = fills['ORD1'].sum()",
        "total = sum(fills)",
        "total = len(fills['ORD1'])"
      ],
      "answer": 0,
      "explanation": "Use `sum()` over the list of fill quantities."
    },
    {
      "category": "VWAP",
      "question": "Initialize `total_value` to zero for VWAP calculation:\n\n```python\n# before looping trades\n```",
      "options": [
        "total_value = 0",
        "total_value = []",
        "total_value = None",
        "total_value = {}"
      ],
      "answer": 0,
      "explanation": "Start the accumulator at zero."
    },
    {
      "category": "VWAP",
      "question": "Initialize `total_size` to zero for VWAP calculation:\n\n```python\n# before looping trades\n```",
      "options": [
        "total_size = 0",
        "total_size = []",
        "total_size = None",
        "total_size = {}"
      ],
      "answer": 0,
      "explanation": "Start the size accumulator at zero."
    },
    {
      "category": "VWAP",
      "question": "Loop over `trades`, unpacking into `price` and `size`:\n\n```python\ntrades = [(10,2), (12,3)]\n# loop here\n```",
      "options": [
        "for price, size in trades:",
        "for trade in trades:\n    price, size = trade",
        "trades.forEach(lambda p,s: ...)",
        "while trades:\n    price, size = trades.pop()"
      ],
      "answer": 0,
      "explanation": "Tuple unpacking in a for-loop."
    },
    {
      "category": "VWAP",
      "question": "Accumulate trade value into `total_value`:\n\n```python\n# inside your loop\n```",
      "options": [
        "total_value += price * size",
        "total_value = price * size",
        "total += price * size",
        "total_value.append(price * size)"
      ],
      "answer": 0,
      "explanation": "Multiply price by size and add to the accumulator."
    },
    {
      "category": "VWAP",
      "question": "Accumulate trade size into `total_size`:\n\n```python\n# inside your loop\n```",
      "options": [
        "total_size += size",
        "total_value += size",
        "size += total_size",
        "total_size.append(size)"
      ],
      "answer": 0,
      "explanation": "Add the size to the size accumulator."
    },
    {
      "category": "VWAP",
      "question": "Calculate VWAP into `vwap`:\n\n```python\n# after loop\n```",
      "options": [
        "vwap = total_value / total_size",
        "vwap = total_size / total_value",
        "vwap = sum(values)/sum(sizes)",
        "vwap = price / size"
      ],
      "answer": 0,
      "explanation": "Divide total traded value by total traded size."
    },
    {
      "category": "Sockets",
      "question": "Import the socket module:\n\n```python\n# top of your script\n```",
      "options": [
        "import socket",
        "import Socket",
        "from socket import *",
        "require('socket')"
      ],
      "answer": 0,
      "explanation": "Use the import statement for Python modules."
    },
    {
      "category": "Sockets",
      "question": "Create a TCP socket assigned to `sock`:\n\n```python\n# after importing socket\n```",
      "options": [
        "sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)",
        "sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)",
        "sock = socket.TCP()",
        "sock = socket.new('TCP')"
      ],
      "answer": 0,
      "explanation": "AF_INET+SOCK_STREAM specifies a TCP socket."
    },
    {
      "category": "Sockets",
      "question": "Connect `sock` to `example.com` on port 5000:\n\n```python\n# after creating sock\n```",
      "options": [
        "sock.connect(('example.com', 5000))",
        "sock.connect('example.com', 5000)",
        "connect(sock, 'example.com', 5000)",
        "sock.open('example.com', 5000)"
      ],
      "answer": 0,
      "explanation": "Use a tuple (host, port) with `connect()`."
    },
    {
      "category": "Sockets",
      "question": "Close the socket `sock`:\n\n```python\n# when you’re done\n```",
      "options": [
        "sock.close()",
        "close(sock)",
        "sock.shutdown()",
        "sock.end()"
      ],
      "answer": 0,
      "explanation": "Call `close()` on the socket object."
    },
    {
      "category": "System Monitoring",
      "question": "Import the `psutil` module:\n\n```python\n# top of your script\n```",
      "options": [
        "import psutil",
        "import psu",
        "from psutil import *",
        "require('psutil')"
      ],
      "answer": 0,
      "explanation": "`psutil` provides system statistics functions."
    },
    {
      "category": "System Monitoring",
      "question": "Get 1-second CPU usage into `cpu`:\n\n```python\n# after importing psutil\n```",
      "options": [
        "cpu = psutil.cpu_percent(interval=1)",
        "cpu = psutil.cpu_percent(1)",
        "cpu = psutil.get_cpu(1)",
        "cpu = psutil.cpu_usage(1)"
      ],
      "answer": 0,
      "explanation": "`cpu_percent()` blocks for the interval and returns usage."
    },
    {
      "category": "System Monitoring",
      "question": "Get virtual memory stats into `mem`:\n\n```python\n# after importing psutil\n```",
      "options": [
        "mem = psutil.virtual_memory()",
        "mem = psutil.mem_info()",
        "mem = psutil.memory()",
        "mem = psutil.get_virtual()"
      ],
      "answer": 0,
      "explanation": "`virtual_memory()` returns a namedtuple with memory stats."
    },
    {
      "category": "System Monitoring",
      "question": "Get root-filesystem disk usage into `disk`:\n\n```python\n# after importing psutil\n```",
      "options": [
        "disk = psutil.disk_usage('/')",
        "disk = psutil.get_disk_usage('/')",
        "disk = psutil.usage('/')",
        "disk = psutil.root_usage()"
      ],
      "answer": 0,
      "explanation": "`disk_usage(path)` returns usage stats for that path."
    },
    {
      "category": "Regex",
      "question": "Import the `re` (regex) module:\n\n```python\n# top of your script\n```",
      "options": [
        "import re",
        "import regex",
        "from re import *",
        "require('re')"
      ],
      "answer": 0,
      "explanation": "Use `import re` for Python’s built-in regex."
    },
    {
      "category": "SQL",
      "question": "Replace `TOP 10` with `TOP 3` in SQL query `query`:\n\n```python\n# query is a string\n```",
      "options": [
        "query = query.replace('TOP 10', 'TOP 3')",
        "query = query.swap('TOP 10', 'TOP 3')",
        "query = re.sub('TOP 10', 'TOP 3', query)",
        "query = query.remove('TOP 10')"
      ],
      "answer": 0,
      "explanation": "`str.replace(old, new)` performs a simple substring swap."
    },
    {
      "category": "SQL & Regex",
      "question": "Use regex to substitute any `TOP N` with `TOP {limit}`:\n\n```python\n# limit is an integer\n```",
      "options": [
        "query = re.sub(r'TOP\\s+\\d+', f'TOP {limit}', query)",
        "query = query.replace(r'TOP\\d+', f'TOP {limit}')",
        "query = regex.sub('TOP.*', f'TOP {limit}', query)",
        "query = re.match(r'TOP', query)"
      ],
      "answer": 0,
      "explanation": "`re.sub()` lets you use patterns and replacement strings."
    },
    {
      "category": "Dict Access",
      "question": "Given `msg = {'8':'FIX.4.2','9':'112','35':'D','55':'IBM','38':'100'}`, extract the symbol:\n\n```python\n# get symbol here\n```",
      "options": [
        "symbol = msg['55']",
        "symbol = msg.get('55')",
        "symbol = msg['symbol']",
        "symbol = msg.Symbol"
      ],
      "answer": 0,
      "explanation": "Tag 55 maps to the Symbol."
    },
    {
      "category": "String Join",
      "question": "Given `items = ['apple','banana','cherry']`, assemble into comma-separated string `csv`:\n\n```python\nitems = ['apple','banana','cherry']\n# build csv here\n```",
      "options": [
        "csv = ','.join(items)",
        "csv = items.join(',')",
        "csv = '-'.join(items)",
        "csv = str(items)"
      ],
      "answer": 0,
      "explanation": "`join()` concatenates list elements with the separator."
    },
    {
      "category": "Looping",
      "question": "Loop over `items` with index `i` and value `item` using `enumerate`:\n\n```python\nitems = ['apple','banana','cherry']\n# loop here\n```",
      "options": [
        "for i, item in enumerate(items):",
        "for item in items():",
        "for i in range(len(items)):",
        "items.foreach(i, item)"
      ],
      "answer": 0,
      "explanation": "`enumerate()` yields pairs of (index, value)."
    },
    {
      "category": "Set Logic",
      "question": "Given `flags = {'A','Z'}`, check if any of `'X'`, `'Y'`, or `'Z'` is present:\n\n```python\nflags = {'A','Z'}\n# test here\n```",
      "options": [
        "if any(f in flags for f in ['X','Y','Z']):",
        "if flags & {'X','Y','Z'}:",
        "if 'Z' in flags:",
        "if flags.contains('Z'):"
      ],
      "answer": 0,
      "explanation": "`any()` short-circuits on the first True."
    },
    {
      "category": "Threading",
      "question": "Launch function `monitor` in a new daemon thread:\n\n```python\n# after defining monitor()\n```",
      "options": [
        "threading.Thread(target=monitor, daemon=True).start()",
        "threading.start_new_thread(monitor, ())",
        "Thread(monitor).start()",
        "monitor_thread = Thread(monitor)"
      ],
      "answer": 0,
      "explanation": "Setting `daemon=True` means the thread won’t block program exit."
    },
    {
      "category": "List Comprehension",
      "question": "Given `lines = ['start service','stop service','start backup']`, collect lines beginning with 'start':\n\n```python\nlines = ['start service','stop service','start backup']\n# build start_lines\n```",
      "options": [
        "start_lines = [l for l in lines if l.startswith('start')]",
        "start_lines = filter(lambda l: l.startswith('start'), lines)",
        "start_lines = [l for l in lines if 'start' in l]",
        "start_lines = [l for l in lines if l[:5] == 'start']"
      ],
      "answer": 0,
      "explanation": "`startswith()` tests the prefix and a comprehension filters accordingly."
    }
  ]
}
